# Examples use:
  - database: spm
  - username: picard
  - password: mYPas$worD

# access postgres
$ psql --host=localhost --dbname=spm --username=picard

# dump sql
$ env PGPASSWORD="mYPas$worD" pg_dump -h localhost -U picard spm > spm.sql